# Техническое задание: Фронтенд для управления AI контекстами

## Общая структура приложения

Приложение представляет собой админ-панель для управления AI контекстами (командами) банковского чат-бота.

---

## 1. МОЗГ (baseBrainContext) - Отдельный раздел

### Описание
`baseBrainContext` - это базовый контекст AI, который используется во ВСЕХ диалогах. Это основная личность бота.

### Пример данных
```json
{
  "baseBrainContext": "Тебя зовут Реня. Ты ИИ‑консультант в СК «Ренессанс». Твоя задача — помогать клиентам подбирать финансовые продукты."
}
```

### API для работы с Мозгом

#### Получение baseBrainContext
```
GET /api/contexts
```
**Ответ:**
```json
{
  "baseBrainContext": "Тебя зовут Реня. Ты ИИ‑консультант в СК «Ренессанс»...",
  "contexts": { ... }
}
```

#### Обновление baseBrainContext
```
PUT /api/contexts/brain
```
**Тело запроса:**
```json
{
  "baseBrainContext": "Новый текст базового контекста"
}
```

### UI для раздела "МОЗГ"

**Расположение:** Отдельный пункт в боковом меню (самый первый или самый важный)

**Содержимое страницы:**
- Заголовок: "МОЗГ" или "Базовый контекст"
- Одно большое текстовое поле (textarea) для редактирования `baseBrainContext`
- Кнопка "Сохранить"
- Описание: "Это базовая личность AI, которая применяется ко всем диалогам"

---

## 2. СЕКЦИИ (Sections) - Группировка команд

### Описание
Команды (contexts) группируются по секциям. Каждая секция - это отдельная категория функционала бота.

### Структура секций

Согласно изображениям и коду, есть следующие секции:

1. **МОЗГ** - отдельный раздел (см. выше)
2. **ОБРАБОТКА ПФП** - секция для обработки персональных финансовых планов
3. **СОЗДАНИЕ ПФП JSON** - секция для создания JSON планов
4. **РАБОТА С ПФП** - секция для работы с планами
5. **ДИАГНОСТИКА** - секция диагностики
6. **НСЖ КОПИЛКА И КЕШБЭК** - продукты НСЖ
7. **NEXTPLAN** - продукт NextPlan
8. **LIFEINSURANCE** - страхование жизни
9. **ИСХОДЯЩИЕ СООБЩЕНИЯ** - шаблоны исходящих сообщений
10. **ПОЛЬЗОВАТЕЛИ** - управление пользователями

### Примеры секций из кода:
- `onboarding` - онбординг клиентов
- `products` - продукты банка
- `planning` - финансовое планирование

---

## 3. СТРУКТУРА КОМАНДЫ (Context)

Каждая команда ОБЯЗАТЕЛЬНО должна содержать 3 поля:

### Обязательные поля:

1. **Ключ команды** (например: `/startpfp`, `/goLife`, `/plan`)
   - Начинается с `/`
   - Уникальный идентификатор

2. **classifier** (Классификатор)
   - Текст, который помогает AI определить, когда переключиться на другую команду
   - Содержит логику маршрутизации между командами
   
   **Пример:**
   ```
   Если клиенту интересно заработать повышенный процент, то напиши команду /goLife
   А если клиенту интересно накопить капитал на свою цель, то напиши команду /plan
   Если клиент пишет, что он хочет Инвестировать, то напиши команду /goLife
   
   Если ничего не подходит, возвращай команду /startpfp
   ```

3. **response** (Ответ)
   - Инструкции для AI, как отвечать клиенту в этом контексте
   - Описание поведения бота
   
   **Пример:**
   ```
   У тебя есть продукты со ставкой до 21% годовых. Но надо узнать, что хочет клиент.
   Спроси клиента, что ему интересно: выгодно приумножить деньги — получить максимальный процент 
   или сделать финансовый план на его цель в среднесрочной или долгосрочной перспективе.
   
   Продукты пока не описывай их много. Надо узнать, что интересно клиенту.
   ```

4. **section** (Секция) - ОБЯЗАТЕЛЬНОЕ поле
   - Определяет, к какой секции относится команда
   - Примеры: `"onboarding"`, `"products"`, `"planning"`

### Полный пример команды:

```json
"/startpfp": {
  "classifier": "\nЕсли клиенту интересно заработать повышенный процент, то напиши команду /goLife\nА если клиенту интересно накопить капитал на свою цель, то напиши команду /plan\nЕсли клиент пишет, что он хочет Инвестировать, то напиши команду /goLife\n\nЕсли ничего не подходит, возвращай команду /startpfp\n",
  "response": "\nУ тебя есть продукты со ставкой до 21% годовых. Но надо узнать, что хочет клиент.\nСпроси клиента, что ему интересно: выгодно приумножить деньги — получить максимальный процент или сделать финансовый план на его цель в среднесрочной или долгосрочной перспективе.\n\nПродукты пока не описывай их много. Надо узнать, что интересно клиенту.\n",
  "section": "onboarding"
}
```

---

## 4. UI СТРУКТУРА ФРОНТЕНДА

### Левая боковая панель (Sidebar)

**Заголовок:** "СЕКЦИИ" или "Разделы"

**Список секций:**
1. МОЗГ (специальный раздел)
2. ОБРАБОТКА ПФП
3. СОЗДАНИЕ ПФП JSON
4. РАБОТА С ПФП
5. ДИАГНОСТИКА
6. НСЖ КОПИЛКА И КЕШБЭК
7. NEXTPLAN
8. LIFEINSURANCE
9. ИСХОДЯЩИЕ СООБЩЕНИЯ
10. ПОЛЬЗОВАТЕЛИ

**Поведение:**
- При клике на секцию - показываются только команды этой секции
- Активная секция подсвечивается
- Секция "МОЗГ" открывает отдельный интерфейс (см. раздел 1)

---

### Основная область (Main Content)

#### Для секции "МОЗГ":
- Большое текстовое поле для редактирования `baseBrainContext`
- Кнопка "Сохранить"

#### Для остальных секций:

**Отображение списка команд:**

Каждая команда отображается как карточка со следующей информацией:

```
┌─────────────────────────────────────────────────┐
│ /startpfp                    [onboarding]       │
│                                    [✏️] [🗑️]    │
├─────────────────────────────────────────────────┤
│ КЛАССИФИКАТОР:                                  │
│ Если клиенту интересно заработать повышенный... │
│                                                 │
│ ОТВЕТ:                                          │
│ У тебя есть продукты со ставкой до 21%...      │
└─────────────────────────────────────────────────┘
```

**Элементы карточки:**
1. **Заголовок:** Ключ команды (например: `/startpfp`)
2. **Тег секции:** Badge с названием секции (например: `onboarding`)
3. **Кнопки действий:**
   - ✏️ Редактировать
   - 🗑️ Удалить
4. **Поля:**
   - "КЛАССИФИКАТОР:" - текст классификатора (можно сворачивать/разворачивать)
   - "ОТВЕТ:" - текст ответа (можно сворачивать/разворачивать)

---

### Форма создания/редактирования команды

**Поля формы:**

1. **Ключ команды** (Command Key)
   - Input text
   - Placeholder: "/commandName"
   - Обязательное поле
   - Должно начинаться с `/`

2. **Секция** (Section)
   - Dropdown select
   - Список всех доступных секций
   - Обязательное поле
   - Автоматически выбрана текущая секция

3. **Классификатор** (Classifier)
   - Textarea (большое текстовое поле)
   - Placeholder: "Опишите условия переключения на другие команды..."
   - Обязательное поле

4. **Ответ** (Response)
   - Textarea (большое текстовое поле)
   - Placeholder: "Опишите, как AI должен отвечать в этом контексте..."
   - Обязательное поле

**Кнопки:**
- "Сохранить" - сохранить команду
- "Отмена" - закрыть форму без сохранения

---

## 5. API ENDPOINTS

### Получить все контексты
```
GET /api/contexts
```
**Ответ:**
```json
{
  "baseBrainContext": "...",
  "contexts": {
    "/start": { ... },
    "/startpfp": { ... }
  }
}
```

### Получить команды конкретной секции
```
GET /api/contexts?section=onboarding
```
**Ответ:**
```json
{
  "/start": {
    "classifier": "...",
    "response": "...",
    "section": "onboarding"
  },
  "/startpfp": {
    "classifier": "...",
    "response": "...",
    "section": "onboarding"
  }
}
```

### Создать новую команду
```
POST /api/contexts
```
**Тело запроса:**
```json
{
  "command": "/newCommand",
  "classifier": "...",
  "response": "...",
  "section": "products"
}
```

### Обновить команду
```
PUT /api/contexts/:command
```
**Тело запроса:**
```json
{
  "classifier": "...",
  "response": "...",
  "section": "products"
}
```

### Удалить команду
```
DELETE /api/contexts/:command
```

### Обновить baseBrainContext
```
PUT /api/contexts/brain
```
**Тело запроса:**
```json
{
  "baseBrainContext": "Новый текст"
}
```

---

## 6. ВАЖНЫЕ ТРЕБОВАНИЯ

### Обязательные поля в каждой команде:
1. ✅ **classifier** - логика маршрутизации
2. ✅ **response** - инструкции для AI
3. ✅ **section** - принадлежность к секции

### Фильтрация по секциям:
- При выборе секции в sidebar показываются ТОЛЬКО команды этой секции
- Секция определяется по полю `"section"` в каждой команде
- Новые команды автоматически получают секцию, которая активна в данный момент

### Валидация:
- Ключ команды должен начинаться с `/`
- Все три поля (classifier, response, section) обязательны
- Ключ команды должен быть уникальным

---

## 7. ПРИМЕР ПОЛНОЙ СТРУКТУРЫ ДАННЫХ

```json
{
  "baseBrainContext": "Тебя зовут Реня. Ты ИИ‑консультант в СК «Ренессанс». Твоя задача — помогать клиентам подбирать финансовые продукты.",
  "contexts": {
    "/start": {
      "classifier": "Логика определения следующей команды...",
      "response": "Инструкции для AI как отвечать...",
      "section": "onboarding"
    },
    "/startpfp": {
      "classifier": "Если клиенту интересно заработать повышенный процент, то напиши команду /goLife...",
      "response": "У тебя есть продукты со ставкой до 21% годовых...",
      "section": "onboarding"
    },
    "/goLife": {
      "classifier": "Если клиент ответил, что ему интересно получать выплаты регулярно...",
      "response": "Ты должна выяснить у клиента, как ему удобно...",
      "section": "products"
    },
    "/plan": {
      "classifier": "Если клиент назвал свою цель...",
      "response": "Отлично! Давайте составим финансовый план...",
      "section": "planning"
    }
  }
}
```

---

## 8. ДИЗАЙН И UX

### Цветовая схема (из изображения):
- Фон: Темный (темно-синий/черный)
- Sidebar: Немного светлее основного фона
- Активная секция: Синий акцент (#4A90E2 или похожий)
- Карточки команд: Темно-серый с легким градиентом
- Текст: Белый/светло-серый
- Акценты: Синий для кнопок и активных элементов

### Типографика:
- Заголовки: Жирный шрифт, uppercase для секций
- Ключи команд: Моноширинный шрифт (monospace) для `/command`
- Основной текст: Sans-serif, читаемый размер

### Интерактивность:
- Hover эффекты на карточках команд
- Плавные переходы при переключении секций
- Анимация при добавлении/удалении команд
- Подтверждение при удалении команды

---

## 9. ЛОГИКА РАБОТЫ

### При загрузке приложения:
1. Загрузить все данные через `GET /api/contexts`
2. Отобразить список секций в sidebar
3. Выбрать первую секцию по умолчанию (или "МОЗГ")
4. Отобразить команды выбранной секции

### При выборе секции "МОЗГ":
1. Показать интерфейс редактирования `baseBrainContext`
2. Загрузить текущее значение из данных
3. При сохранении - отправить `PUT /api/contexts/brain`

### При выборе другой секции:
1. Отфильтровать команды по полю `section`
2. Отобразить только команды этой секции
3. Показать кнопку "Добавить команду"

### При создании команды:
1. Открыть форму создания
2. Автоматически установить текущую секцию
3. При сохранении - отправить `POST /api/contexts`
4. Обновить список команд

### При редактировании команды:
1. Открыть форму с заполненными данными
2. Разрешить изменить секцию
3. При сохранении - отправить `PUT /api/contexts/:command`
4. Обновить список команд

### При удалении команды:
1. Показать подтверждение
2. При подтверждении - отправить `DELETE /api/contexts/:command`
3. Удалить команду из списка

---

## 10. ТЕХНИЧЕСКИЙ СТЕК (рекомендации)

- **Frontend Framework:** React или Vue.js
- **Styling:** CSS Modules или Styled Components (темная тема)
- **HTTP Client:** Axios или Fetch API
- **State Management:** React Context API или Vuex (для управления состоянием)
- **UI Components:** Можно использовать готовые компоненты, но адаптировать под темную тему

---

## ИТОГОВАЯ СТРУКТУРА ФАЙЛОВ (пример)

```
/src
  /components
    Sidebar.jsx          # Боковое меню с секциями
    BrainEditor.jsx      # Редактор baseBrainContext
    CommandList.jsx      # Список команд секции
    CommandCard.jsx      # Карточка одной команды
    CommandForm.jsx      # Форма создания/редактирования
  /services
    api.js               # API вызовы
  /utils
    validators.js        # Валидация данных
  App.jsx                # Главный компонент
  index.css              # Глобальные стили
```

---

## ПРИОРИТЕТЫ РАЗРАБОТКИ

1. ✅ **Высокий приоритет:**
   - Раздел "МОЗГ" с редактированием baseBrainContext
   - Sidebar с секциями
   - Фильтрация команд по секциям
   - Обязательное поле `section` в каждой команде

2. ⚠️ **Средний приоритет:**
   - Форма создания/редактирования команд
   - Валидация данных
   - Подтверждение удаления

3. 📌 **Низкий приоритет:**
   - Анимации и переходы
   - Расширенные фильтры
   - Поиск по командам
