# User Context Implementation

## Обзор

Реализована функциональность **персонального контекста пользователя** (`userContext`), которая позволяет администратору добавлять индивидуальную информацию о каждом пользователе, которая автоматически передается AI при генерации ответов.

## Пример использования

Администратор может добавить контекст для пользователя:
```
Отдел Закупок. Руководитель - Баринов Виктор. Виктор поставил задачу Людмиле что нужно подготовить план закупок на 2-й квартал
```

После этого AI будет "знать" эту информацию при общении с данным пользователем и сможет давать более персонализированные ответы.

## Архитектура

### 1. База данных (`db.js`)

**Добавлено поле в таблицу `users`:**
```sql
user_context TEXT
```

**Миграция:**
- Автоматически добавляется при запуске, если поле отсутствует
- Существующие пользователи получают `NULL` в `user_context`

### 2. Функции работы с контекстом (`user.js`)

**Новые функции:**
- `getUserContext(userId)` - получить контекст пользователя
- `setUserContext(userId, context)` - установить/обновить контекст
- `deleteUserContext(userId)` - удалить контекст (установить NULL)

**Обновлено:**
- `listUsers()` - теперь возвращает `user_context` в списке пользователей

### 3. Формирование контекста для AI (`context.js`)

**Функция `getResponseContext(botId, command, userId)`:**

Теперь принимает опциональный параметр `userId` и формирует финальный контекст в следующем порядке:

```
[baseBrainContext бота]
---
[response контекст команды]
---
Контекст пользователя:
[userContext]
```

**Логика:**
- Если `userId` не передан - контекст пользователя не добавляется
- Если `userContext` пустой или NULL - секция не добавляется
- Контекст добавляется только в `askAI()`, НЕ в `classifyIntent()`

### 4. Admin API (`index.js`)

**Новые endpoints:**

#### GET `/api/admin/users/:id/context`
Получить контекст пользователя

**Response:**
```json
{
  "userId": "123456789",
  "userContext": "Отдел Закупок..."
}
```

#### PUT `/api/admin/users/:id/context`
Установить/обновить контекст

**Request:**
```json
{
  "userContext": "Отдел Закупок. Руководитель - Баринов Виктор..."
}
```

**Response:**
```json
{
  "success": true,
  "message": "User context updated"
}
```

#### DELETE `/api/admin/users/:id/context`
Удалить контекст

**Response:**
```json
{
  "success": true,
  "message": "User context deleted"
}
```

### 5. Интеграция с ботами

**Обновлены вызовы `getResponseContext()`:**

- `telegram.js` (строка 111): передается `chatId` как `userId`
- `api.js` (строка 89): передается `userId` из запроса

**Пример:**
```javascript
const responseContext = await getResponseContext(botId, newCommand, chatId);
```

## Особенности реализации

### ✅ Что сделано:

1. **База данных:**
   - Поле `user_context TEXT` в таблице `users`
   - Автоматическая миграция при старте

2. **Backend функции:**
   - CRUD операции для `userContext`
   - Интеграция в формирование контекста AI

3. **API endpoints:**
   - GET, PUT, DELETE для управления контекстом
   - Полная документация в `MULTI_BOT_API.md`

4. **Интеграция с AI:**
   - Контекст автоматически добавляется в промпт
   - Работает для Telegram ботов и Partner API

### 🔧 Что нужно сделать на фронтенде:

1. **UI для редактирования контекста:**
   - Текстовое поле или модальное окно
   - Кнопки "Сохранить" / "Удалить"

2. **Интеграция с API:**
   ```javascript
   // Получить контекст
   GET /api/admin/users/${userId}/context
   
   // Сохранить контекст
   PUT /api/admin/users/${userId}/context
   Body: { userContext: "..." }
   
   // Удалить контекст
   DELETE /api/admin/users/${userId}/context
   ```

3. **Отображение в списке пользователей:**
   - Индикатор наличия контекста (например, иконка)
   - Превью контекста при наведении

## Тестирование

### Ручное тестирование через API:

1. **Установить контекст:**
```bash
curl -X PUT http://localhost:3000/api/admin/users/123456789/context \
  -H "Authorization: Basic <credentials>" \
  -H "Content-Type: application/json" \
  -d '{"userContext": "Отдел Закупок. Руководитель - Баринов Виктор."}'
```

2. **Получить контекст:**
```bash
curl http://localhost:3000/api/admin/users/123456789/context \
  -H "Authorization: Basic <credentials>"
```

3. **Написать сообщение боту** и проверить логи:
```
[DEBUG] User Context Found: true, Length: 52
```

4. **Удалить контекст:**
```bash
curl -X DELETE http://localhost:3000/api/admin/users/123456789/context \
  -H "Authorization: Basic <credentials>"
```

## Логирование

При каждом запросе к AI в консоли отображается:
```
[DEBUG] getResponseContext botId=1 command=/start userId=123456789
[DEBUG] Base Context Found: true, Length: 150
[DEBUG] Command Response Found: true, Length: 200
[DEBUG] User Context Found: true, Length: 52
```

## Безопасность

- ✅ Все endpoints защищены Basic Auth
- ✅ Валидация типа данных (`userContext` должен быть строкой)
- ✅ SQL injection защита через prepared statements
- ✅ Контекст пользователя глобальный (не привязан к боту)

## Совместимость

- ✅ Работает с multi-bot архитектурой
- ✅ Обратная совместимость (если `userId` не передан, контекст не добавляется)
- ✅ Работает для Telegram ботов и Partner API
- ✅ Автоматическая миграция существующих БД

## Документация

Полная документация API доступна в:
- `MULTI_BOT_API.md` - endpoints 13, 14, 15
- Database schema обновлена (таблица `users`)
